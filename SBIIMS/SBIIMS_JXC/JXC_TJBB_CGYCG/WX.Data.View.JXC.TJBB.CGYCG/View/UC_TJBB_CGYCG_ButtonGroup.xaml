<UserControl x:Class="WX.Data.View.UC_TJBB_CGYCG_ButtonGroup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource UserControlAutoSizeStyle}">
    <UserControl.Resources>
        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonLeftNormalStyle}" />
        <Style TargetType="{x:Type ColumnDefinition}" BasedOn="{StaticResource ColumnDefinitionFixedStyle}" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="导出" />
        <Button Grid.Column="1" Content="打印" />
    </Grid>
</UserControl>
